import React from 'react';
import Icon from 'react-native-vector-icons/Fontisto';

const FontistoIcons = ({name, size, color}) => {
  return <Icon name={name} size={size} color={color} />;
};

export default FontistoIcons;
